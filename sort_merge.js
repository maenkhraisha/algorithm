function mergeSort(arr1, arr2) {
  
  
  function merge(arr1, arr2) {
    let l = arr1.length + arr2.length;
    let arr = [l];

    for (let i = 0; i < arr1.length || i < arr2.length; i++) {
      
    }
  }

  merge(arr1,arr2);
}

console.log(mergeSort([2, 3, 4], [6, 5, 7]));
